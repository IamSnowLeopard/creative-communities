import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#280003", // Change this to your desired primary color
    },
    secondary: {
      main: "#FFaf1b", // Change this to your desired secondary color
    },
  },
});

export default theme;
